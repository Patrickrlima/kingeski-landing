/**
 * Depoimentos reais de clientes — proposital vazio (não inventamos
 * avaliações). Adicione aqui nesse formato quando tiver os textos reais
 * (do Google, por exemplo):
 *
 * { author: 'Nome do cliente', text: 'Trecho do depoimento real.', rating: 5 }
 *
 * Enquanto vazio, a seção "Avaliações" mostra só a nota geral do Google
 * (configurável abaixo) com um botão para ver as avaliações completas lá.
 */
const reviews = [];

// TODO: opcional — se quiser mostrar a nota média do Google aqui, preencha
// (não deixe um número desatualizado; confira no perfil antes de publicar).
export const googleRating = {
  // score: 5.0,
  // count: 22,
};

export default reviews;
